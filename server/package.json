{
 "name": "grid-app-server",
 "version": "1.0.0",
 "description": "JSON Server for Grid App Data",
 "main": "server.js",
 "scripts": {
  "start": "json-server --watch db.json --port 3001",
  "dev": "json-server --watch db.json --port 3001 --delay 1000"
 },
 "dependencies": {
  "json-server": "^1.0.0-beta.3",
  "dotenv": "^16.3.1"
 },
 "devDependencies": {
  "nodemon": "^3.0.1"
 }
}
